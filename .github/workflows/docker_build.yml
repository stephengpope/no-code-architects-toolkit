name: OIDC CD

on:
  push:
    branches:
      - main

permissions:
  id-token: write
  contents: read

jobs:
  deploy_container:
    name: Deploy Container
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v1
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up Docker to Authenticate with Artifact Registry
        run: |
          echo "Configuring Docker authentication for Artifact Registry..."
          gcloud auth configure-docker us-east1-docker.pkg.dev
          gcloud auth print-access-token | docker login -u oauth2accesstoken --password-stdin https://us-east1-docker.pkg.dev

      - name: Pull Image from Amazon ECR Public
        run: |
          echo "Pulling image from Amazon ECR Public..."
          docker pull public.ecr.aws/t6d9z0z5/no-code-architects-toolkit:latest

      - name: Tag Image for Google Artifact Registry
        run: |
          echo "Tagging image for Google Artifact Registry..."
          docker tag public.ecr.aws/t6d9z0z5/no-code-architects-toolkit:latest \
          us-east1-docker.pkg.dev/csagi-439110/no-code-architects-toolkit/no-code-architects-toolkit:latest

      - name: Push Image to Google Artifact Registry
        run: |
          echo "Pushing image to Google Artifact Registry..."
          docker push us-east1-docker.pkg.dev/csagi-439110/no-code-architects-toolkit/no-code-architects-toolkit:latest